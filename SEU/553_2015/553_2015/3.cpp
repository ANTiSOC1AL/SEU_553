/*
使用随机数函数srand()和time()生成随机数，来模拟掷骰子。</br>
（1）分别投掷两次，将两次所得的值，求出其和值，并写入二维数组A，将数组的行存放第一次投掷的结果，数组的列存放第二次投掷的结果。</br>
（2）如果投掷1000次，将其和值出现的次数写入二维数组B。</br>
 没看懂这题目什么意思 放弃了
*/
#include<iostream>
#include<time.h>
using namespace std;

void rollTwice(int** a) {

}

int main() {
	//生成种子
	srand(time(nullptr));
	int A[2][2] = { 0 };
}